services:
  - type: web
    name: pro-scanner-uat
    runtime: static
    branch: main
    buildCommand: npm ci && npm run build
    staticPublishPath: .
    autoDeployTrigger: commit
    headers:
      - path: /tailwind.min.css
        name: Cache-Control
        value: public, max-age=3600, stale-while-revalidate=86400
      - path: /index.html
        name: Cache-Control
        value: no-cache
